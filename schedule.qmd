---
title: Schedule
---

```{r}
fizzbuzz <- function(n)
{
    ifelse(is.na(x <- rep(c(NA, NA, "fizz", NA, "buzz", "fizz", NA, NA, "fizz", "buzz", NA, "fizz", NA, NA, "fizzbuzz"), length.out = n)), seq(n), x)
}

fizzbuzz(100)
```

